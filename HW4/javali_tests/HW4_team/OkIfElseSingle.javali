/* Test a simple if else statement. */
class Main {

    void main() {
        int i;
        i = 1;
        if (i < 3) {
            write(i);
            writeln();
        } else {
            write(10);
            writeln();
        }
   }
}
